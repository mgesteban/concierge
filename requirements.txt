# Core
anthropic>=0.40.0
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Supabase + embeddings (governance KB)
supabase>=2.7.0
voyageai>=0.2.0

# Twilio (voice + SMS)
twilio>=9.0.0

# Speech stack (voice pipeline)
deepgram-sdk>=3.5.0
elevenlabs>=1.5.0

# HTTP client for webhooks & MCP integrations
httpx>=0.27.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
